# 📡 Real-Time API Эндпоинты

## 🔌 WebSocket Подключение

### URL для подключения:
- **WebSocket URL**: `ws://localhost:4000/graphql`
- **HTTP URL**: `http://localhost:4000/graphql`

---

## 🔐 Аутентификация WebSocket

При подключении к WebSocket необходимо передать JWT токен в параметрах подключения.

### Параметры подключения (connectionParams):

Сервер поддерживает **три формата** передачи токена:

#### Формат 1 (Рекомендуемый):
```json
{
  "authorization": "Bearer <your_jwt_token>"
}
```

#### Формат 2:
```json
{
  "token": "<your_jwt_token>"
}
```

#### Формат 3:
```json
{
  "Authorization": "Bearer <your_jwt_token>"
}
```

### ⚠️ Важно:

1. **Токен должен быть строкой** - не передавайте число или объект
2. **Для формата 1 и 3**: используйте префикс `Bearer ` (с пробелом после Bearer)
3. **Для формата 2**: передавайте только токен без префикса `Bearer`
4. **Токен должен быть валидным JWT** - полученным через `register` или `login`

### Как получить токен:
Токен получается при регистрации или входе через GraphQL mutations:
- `register` - возвращает `token` в `AuthPayload`
- `login` - возвращает `token` в `AuthPayload`

### Примеры правильного использования:

#### ✅ Правильно:
```javascript
// Вариант 1
connectionParams: {
  authorization: `Bearer ${token}`
}

// Вариант 2
connectionParams: {
  token: token
}

// Вариант 3 (функция)
connectionParams: () => ({
  authorization: `Bearer ${getToken()}`
})
```

#### ❌ Неправильно:
```javascript
// НЕ передавайте число
connectionParams: {
  token: 12345
}

// НЕ передавайте объект
connectionParams: {
  token: { value: token }
}

// НЕ забывайте пробел после Bearer
connectionParams: {
  authorization: `Bearer${token}`  // ❌ Нет пробела
}
```

---

## 📨 GraphQL Subscription: messageAdded

### Назначение
Подписка на новые сообщения в реальном времени для конкретного чата.

### Схема

```graphql
subscription {
  messageAdded(chatId: ID!): Message!
}
```

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `chatId` | `ID!` | Да | ID чата, для которого нужно получать сообщения |

### Возвращаемые данные

```graphql
type Message {
  id: ID!
  chat: Chat!
  sender: User!
  content: String
  imageUrl: String
  createdAt: String!
  updatedAt: String!
}
```

### Пример запроса

```graphql
subscription OnMessageAdded {
  messageAdded(chatId: "507f1f77bcf86cd799439020") {
    id
    content
    imageUrl
    sender {
      id
      username
      email
    }
    chat {
      id
      type
      name
    }
    createdAt
  }
}
```

### Пример ответа

```json
{
  "data": {
    "messageAdded": {
      "id": "507f1f77bcf86cd799439030",
      "content": "Привет!",
      "imageUrl": null,
      "sender": {
        "id": "507f1f77bcf86cd799439011",
        "username": "john_doe",
        "email": "john@example.com"
      },
      "chat": {
        "id": "507f1f77bcf86cd799439020",
        "type": "DM",
        "name": null
      },
      "createdAt": "2024-01-01T13:00:00.000Z",
      "updatedAt": "2024-01-01T13:00:00.000Z"
    }
  }
}
```

### Когда приходят события

Событие `messageAdded` приходит автоматически когда:
1. Любой участник чата отправляет новое сообщение через mutation `sendMessage`
2. Сообщение успешно сохранено в базе данных
3. Пользователь является участником этого чата

### Ошибки

| Ошибка | Код | Описание |
|--------|-----|----------|
| `Authentication required` | `UNAUTHENTICATED` | Токен не предоставлен или невалидный |
| `Chat not found` | `BAD_USER_INPUT` | Чат с указанным ID не существует |
| `You are not a participant of this chat` | `FORBIDDEN` | Пользователь не является участником чата |

---

## 💬 Mutation: sendMessage

### Назначение
Отправка сообщения в чат. После успешной отправки все подписчики на `messageAdded` для этого чата автоматически получат событие.

### Схема

```graphql
mutation {
  sendMessage(input: SendMessageInput!): Message!
}

input SendMessageInput {
  chatId: ID!
  content: String
  imageUrl: String
}
```

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `chatId` | `ID!` | Да | ID чата, в который отправляется сообщение |
| `content` | `String` | Нет* | Текст сообщения |
| `imageUrl` | `String` | Нет* | URL изображения (получается через `/api/image`) |

*Примечание: Должен быть указан хотя бы один из параметров (`content` или `imageUrl`)

### Пример запроса (текстовое сообщение)

```graphql
mutation SendMessage {
  sendMessage(input: {
    chatId: "507f1f77bcf86cd799439020"
    content: "Привет всем!"
  }) {
    id
    content
    sender {
      id
      username
    }
    createdAt
  }
}
```

### Пример запроса (изображение)

```graphql
mutation SendImage {
  sendMessage(input: {
    chatId: "507f1f77bcf86cd799439020"
    imageUrl: "/uploads/image-1234567890.jpg"
  }) {
    id
    imageUrl
    sender {
      id
      username
    }
    createdAt
  }
}
```

### Пример запроса (текст + изображение)

```graphql
mutation SendMessageWithImage {
  sendMessage(input: {
    chatId: "507f1f77bcf86cd799439020"
    content: "Смотри, что я нашел!"
    imageUrl: "/uploads/image-1234567890.jpg"
  }) {
    id
    content
    imageUrl
    sender {
      id
      username
    }
    createdAt
  }
}
```

### Возвращаемые данные

```json
{
  "data": {
    "sendMessage": {
      "id": "507f1f77bcf86cd799439030",
      "content": "Привет всем!",
      "imageUrl": null,
      "sender": {
        "id": "507f1f77bcf86cd799439011",
        "username": "john_doe"
      },
      "chat": {
        "id": "507f1f77bcf86cd799439020",
        "type": "DM"
      },
      "createdAt": "2024-01-01T13:00:00.000Z",
      "updatedAt": "2024-01-01T13:00:00.000Z"
    }
  }
}
```

### Ошибки

| Ошибка | Код | Описание |
|--------|-----|----------|
| `Authentication required` | `UNAUTHENTICATED` | Токен не предоставлен или невалидный |
| `Message must have content or image` | `BAD_USER_INPUT` | Не указаны ни content, ни imageUrl |
| `Chat not found` | `BAD_USER_INPUT` | Чат с указанным ID не существует |
| `You are not a participant of this chat` | `FORBIDDEN` | Пользователь не является участником чата |

### Важно

После успешной отправки сообщения:
1. Сообщение сохраняется в базе данных
2. Автоматически публикуется событие в канал `MESSAGE_ADDED_<chatId>`
3. Все подписчики на subscription `messageAdded` для этого чата получают новое сообщение в реальном времени

---

## 📥 Query: messages

### Назначение
Получение истории сообщений чата. Используется для загрузки существующих сообщений при открытии чата.

### Схема

```graphql
query {
  messages(chatId: ID!, limit: Int, offset: Int): [Message!]!
}
```

### Параметры

| Параметр | Тип | Обязательный | По умолчанию | Описание |
|----------|-----|--------------|--------------|----------|
| `chatId` | `ID!` | Да | - | ID чата |
| `limit` | `Int` | Нет | `50` | Количество сообщений для загрузки |
| `offset` | `Int` | Нет | `0` | Смещение для пагинации |

### Пример запроса

```graphql
query GetMessages {
  messages(
    chatId: "507f1f77bcf86cd799439020"
    limit: 50
    offset: 0
  ) {
    id
    content
    imageUrl
    sender {
      id
      username
    }
    createdAt
  }
}
```

### Пример ответа

```json
{
  "data": {
    "messages": [
      {
        "id": "507f1f77bcf86cd799439030",
        "content": "Привет!",
        "imageUrl": null,
        "sender": {
          "id": "507f1f77bcf86cd799439011",
          "username": "john_doe"
        },
        "createdAt": "2024-01-01T13:00:00.000Z"
      },
      {
        "id": "507f1f77bcf86cd799439031",
        "content": "Как дела?",
        "imageUrl": null,
        "sender": {
          "id": "507f1f77bcf86cd799439012",
          "username": "jane_doe"
        },
        "createdAt": "2024-01-01T13:05:00.000Z"
      }
    ]
  }
}
```

### Ошибки

| Ошибка | Код | Описание |
|--------|-----|----------|
| `Authentication required` | `UNAUTHENTICATED` | Токен не предоставлен или невалидный |
| `Chat not found` | `BAD_USER_INPUT` | Чат с указанным ID не существует |
| `You are not a participant of this chat` | `FORBIDDEN` | Пользователь не является участником чата |

### Примечания

- Сообщения возвращаются в хронологическом порядке (старые первыми)
- Для загрузки следующих сообщений используйте `offset: 50`, `offset: 100` и т.д.
- Новые сообщения, приходящие через subscription, нужно добавлять к этому списку

---

## 🔄 Как работает Real-Time

### Полный цикл:

1. **Пользователь A подключается к WebSocket**
   - Подключение: `ws://localhost:4000/graphql`
   - Передача токена в `connectionParams`
   - Подписка: `subscription { messageAdded(chatId: "123") }`

2. **Пользователь B отправляет сообщение**
   - Mutation: `sendMessage(input: { chatId: "123", content: "Hello" })`
   - Сервер сохраняет сообщение в БД
   - Сервер публикует событие: `pubsub.publish("MESSAGE_ADDED_123", { messageAdded: ... })`

3. **Пользователь A получает сообщение**
   - WebSocket автоматически доставляет событие
   - Данные приходят в формате GraphQL response
   - Пользователь A видит сообщение мгновенно

### Каналы событий

Каждый чат имеет свой уникальный канал:
- Чат с ID `123` → канал `MESSAGE_ADDED_123`
- Чат с ID `456` → канал `MESSAGE_ADDED_456`

Подписчики получают события только для своего канала.

---

## 📋 Сценарии использования

### Сценарий 1: Открытие чата

1. Загрузить историю сообщений:
   ```graphql
   query { messages(chatId: "123", limit: 50) }
   ```

2. Подписаться на новые сообщения:
   ```graphql
   subscription { messageAdded(chatId: "123") }
   ```

3. Новые сообщения будут приходить автоматически через subscription

### Сценарий 2: Отправка сообщения

1. Отправить сообщение:
   ```graphql
   mutation { sendMessage(input: { chatId: "123", content: "Hello" }) }
   ```

2. Сообщение автоматически появится у всех подписчиков через subscription

3. Отправитель также получает ответ от mutation (можно показать в UI сразу)

### Сценарий 3: Несколько чатов одновременно

Для каждого чата нужна отдельная подписка:

```graphql
# Подписка на чат 1
subscription { messageAdded(chatId: "123") }

# Подписка на чат 2
subscription { messageAdded(chatId: "456") }
```

Каждая подписка работает независимо.

---

## ⚠️ Важные моменты

### 1. WebSocket vs HTTP

- **Subscriptions** работают **только через WebSocket**
- **Queries и Mutations** работают через **HTTP**
- WebSocket нужен только для получения данных в реальном времени

### 2. Аутентификация

- Токен передается при подключении WebSocket в `connectionParams`
- Токен также нужен в HTTP заголовках для queries и mutations
- Формат: `Authorization: Bearer <token>`

### 3. Переподключение

- При разрыве соединения нужно переподключиться
- При переподключении нужно снова подписаться на subscriptions
- Старые сообщения можно загрузить через query `messages`

### 4. Производительность

- Каждая подписка создает отдельный канал
- Подписки на несколько чатов = несколько каналов
- Это нормально и работает эффективно

### 5. Безопасность

- Проверка прав доступа происходит при подписке
- Только участники чата могут подписаться на его сообщения
- Только участники чата могут отправлять сообщения

---

## 🔍 Тестирование через GraphQL Playground

### 1. Открыть Playground

Перейдите в браузере: `http://localhost:4000/graphql`

### 2. Подключиться к WebSocket

В Playground есть вкладка "Subscriptions". Там можно:
- Указать WebSocket URL: `ws://localhost:4000/graphql`
- Передать токен в connectionParams
- Выполнить subscription запрос

### 3. Тестирование

**Вкладка 1 (Subscription):**
```graphql
subscription {
  messageAdded(chatId: "your-chat-id") {
    id
    content
    sender {
      username
    }
  }
}
```

**Вкладка 2 (Mutation):**
```graphql
mutation {
  sendMessage(input: {
    chatId: "your-chat-id"
    content: "Test message"
  }) {
    id
    content
  }
}
```

После отправки mutation, в первой вкладке автоматически появится новое сообщение.

---

## 📊 Структура данных

### Message

```graphql
type Message {
  id: ID!              # Уникальный идентификатор
  chat: Chat!          # Чат, к которому относится сообщение
  sender: User!        # Отправитель
  content: String      # Текст сообщения (может быть null)
  imageUrl: String     # URL изображения (может быть null)
  createdAt: String!   # Дата создания (ISO 8601)
  updatedAt: String!   # Дата обновления (ISO 8601)
}
```

### User

```graphql
type User {
  id: ID!
  username: String!
  email: String!
  createdAt: String!
  updatedAt: String!
}
```

### Chat

```graphql
type Chat {
  id: ID!
  name: String
  type: ChatType!      # "DM" или "GROUP"
  participants: [User!]!
  createdBy: User!
  createdAt: String!
  updatedAt: String!
}
```

---

## 🐛 Обработка ошибок

### ⚠️ Ошибка 1002 (Protocol Error) - Быстрое решение

Если вы получаете ошибку **"Connection closed with Protocol Error (1002)"**, это обычно означает проблему с форматом `connectionParams`.

#### ✅ Решение:

1. **Проверьте формат токена в connectionParams:**
   ```javascript
   // ✅ ПРАВИЛЬНО - используйте один из этих форматов:
   
   // Вариант 1 (рекомендуется):
   connectionParams: {
     authorization: `Bearer ${token}`  // Обратите внимание на пробел после Bearer!
   }
   
   // Вариант 2:
   connectionParams: {
     token: token  // Только токен, без Bearer
   }
   ```

2. **Убедитесь, что токен - это строка:**
   ```javascript
   // ✅ ПРАВИЛЬНО
   const token = "eyJhbGciOiJIUzI1NiIs..."
   
   // ❌ НЕПРАВИЛЬНО
   const token = 12345  // Число
   const token = { value: "..." }  // Объект
   ```

3. **Проверьте, что токен валидный:**
   - Токен должен быть получен через `login` или `register` mutation
   - Токен не должен быть истекшим
   - Токен должен быть полным (не обрезанным)

4. **Если используете функцию для connectionParams:**
   ```javascript
   // ✅ ПРАВИЛЬНО
   connectionParams: () => ({
     authorization: `Bearer ${getToken()}`
   })
   
   // Убедитесь, что функция возвращает объект, а не undefined
   ```

### Ошибки подключения WebSocket

- **Проверьте URL**: должен быть `ws://`, а не `http://`
- **Проверьте токен**: должен быть валидный JWT токен
- **Проверьте сервер**: убедитесь, что сервер запущен
- **Проверьте формат connectionParams**: см. раздел выше

### Ошибки подписки

- **Chat not found**: проверьте, что chatId правильный
- **You are not a participant**: пользователь не является участником чата
- **Authentication required**: токен невалидный или не передан

### Ошибки отправки сообщения

- **Message must have content or image**: нужно указать хотя бы одно поле
- **Chat not found**: чат не существует
- **You are not a participant**: нет прав на отправку в этот чат

---

## ⚠️ WebSocket Close Code 1002

### Что это такое?

**Код 1002** - это WebSocket Close Code, который означает **"Protocol Error"** (Ошибка протокола).

Это означает, что одно из соединений (клиент или сервер) обнаружило нарушение протокола WebSocket и закрыло соединение.

### Когда возникает код 1002?

Код 1002 возникает в следующих случаях:

1. **Некорректный формат сообщения**
   - Отправка данных в неправильном формате
   - Нарушение структуры WebSocket фрейма
   - Неправильная кодировка данных

2. **Нарушение протокола GraphQL Subscriptions**
   - Неправильный формат subscription запроса
   - Отправка невалидного GraphQL запроса через WebSocket
   - Нарушение протокола `subscriptions-transport-ws`

3. **Проблемы с connectionParams**
   - Неправильный формат передачи параметров подключения
   - Некорректная структура объекта `connectionParams`

4. **Несовместимость версий протокола**
   - Клиент использует одну версию протокола, сервер - другую
   - Проблемы с библиотеками `subscriptions-transport-ws` или `graphql-ws`

### Примеры ситуаций, когда может возникнуть 1002:

#### ❌ Неправильный формат connectionParams:

```javascript
// Неправильно - объект не в том формате
connectionParams: {
  token: token  // Может вызвать проблему, если формат не ожидается
}

// Правильно
connectionParams: () => ({
  authorization: `Bearer ${token}`
})
```

#### ❌ Неправильный GraphQL запрос через WebSocket:

```graphql
# Неправильно - синтаксическая ошибка
subscription {
  messageAdded(chatId: "123"  # Отсутствует закрывающая скобка
}

# Правильно
subscription {
  messageAdded(chatId: "123") {
    id
    content
  }
}
```

#### ❌ Проблемы с кодировкой данных:

- Отправка бинарных данных в текстовом формате
- Неправильная сериализация JSON

### Как обработать код 1002?

#### На клиенте:

```javascript
// Apollo Client
const wsLink = new GraphQLWsLink(
  createClient({
    url: 'ws://localhost:4000/graphql',
    on: {
      error: (error) => {
        if (error.code === 1002) {
          console.error('Protocol error:', error);
          // Переподключиться
          // Проверить формат запросов
        }
      },
      closed: (event) => {
        if (event.code === 1002) {
          console.error('Connection closed with protocol error');
          // Обработать ошибку протокола
        }
      },
    },
  })
);
```

#### На сервере:

В текущей реализации сервер логирует ошибки, но не закрывает соединение с кодом 1002 явно. Если возникает 1002, это обычно означает проблему на стороне клиента или несовместимость протоколов.

### Как исправить?

#### Шаг 1: Проверьте формат connectionParams

**Самый частый случай ошибки 1002** - неправильный формат `connectionParams`.

```javascript
// ✅ ПРАВИЛЬНО - используйте один из этих форматов:

// Вариант 1 (рекомендуется):
connectionParams: {
  authorization: `Bearer ${token}`  // ВАЖНО: пробел после Bearer!
}

// Вариант 2:
connectionParams: {
  token: token  // Только токен, без Bearer
}

// Вариант 3 (если используете функцию):
connectionParams: () => ({
  authorization: `Bearer ${getToken()}`
})
```

**Проверьте:**
- ✅ Токен - это строка (не число, не объект)
- ✅ Если используете `authorization`, то формат: `"Bearer <token>"` (с пробелом!)
- ✅ Если используете `token`, то только токен без префикса `Bearer`
- ✅ Функция `connectionParams` возвращает объект (не `undefined`)

#### Шаг 2: Проверьте GraphQL запросы

- Убедитесь, что синтаксис правильный
- Проверьте, что используете правильные типы данных
- Убедитесь, что subscription запрос валидный

#### Шаг 3: Проверьте совместимость протоколов

**Важно:** Сервер использует `subscriptions-transport-ws` (старый протокол).

Если вы используете на клиенте `graphql-ws` (новый протокол), это может вызвать ошибку 1002.

**Решение:**
- Используйте на клиенте `subscriptions-transport-ws` вместо `graphql-ws`
- Или убедитесь, что библиотека клиента совместима с `subscriptions-transport-ws`

#### Шаг 4: Проверьте формат данных

- Все данные должны быть в формате JSON
- Правильная кодировка UTF-8
- Токен не должен содержать специальных символов, которые нарушают JSON

### Другие WebSocket Close Codes:

| Код | Название | Описание |
|-----|----------|----------|
| 1000 | Normal Closure | Нормальное закрытие соединения |
| 1001 | Going Away | Сервер уходит или браузер закрывает страницу |
| 1002 | **Protocol Error** | **Ошибка протокола (наш случай)** |
| 1003 | Unsupported Data | Неподдерживаемый тип данных |
| 1006 | Abnormal Closure | Аномальное закрытие (без close frame) |
| 1007 | Invalid Data | Невалидные данные |
| 1008 | Policy Violation | Нарушение политики |
| 1009 | Message Too Big | Сообщение слишком большое |
| 1011 | Internal Error | Внутренняя ошибка сервера |

### В контексте нашего проекта:

В нашем проекте код 1002 может возникнуть если:

1. **Клиент передает неправильный формат токена:**
   ```javascript
   // ❌ Неправильно
   connectionParams: { token: 12345 }  // Число вместо строки
   
   // ✅ Правильно
   connectionParams: { authorization: `Bearer ${token}` }
   ```

2. **GraphQL subscription запрос имеет синтаксическую ошибку**

3. **Несовместимость между клиентом и сервером:**
   - Клиент использует `graphql-ws`, а сервер `subscriptions-transport-ws`
   - Или наоборот

4. **Проблемы с сериализацией данных:**
   - Специальные символы в сообщениях
   - Неправильная кодировка

### Рекомендации:

1. **Всегда проверяйте формат данных перед отправкой**
2. **Используйте одинаковые версии протоколов на клиенте и сервере**
3. **Обрабатывайте ошибки подключения и переподключайтесь**
4. **Логируйте ошибки для отладки**

---

## 📚 Связанные эндпоинты

Для полноценной работы с чатами также используются:

- **`query chats`** - список всех чатов пользователя
- **`query chat(id: ID!)`** - информация о конкретном чате
- **`mutation createDM(participantId: ID!)`** - создание личного чата
- **`mutation createGroupChat(input: CreateGroupChatInput!)`** - создание группового чата
- **`POST /api/image`** - загрузка изображения для сообщения

Подробнее см. основную документацию API.

---

**Готово! Теперь вы знаете все про Real-Time API эндпоинты.** 🚀

