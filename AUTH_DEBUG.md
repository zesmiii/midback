# Отладка проблем аутентификации

## 🔍 Проверка проблем

### Проблема: "Invalid email or password"

**Шаги для отладки:**

1. **Проверьте, что пользователь существует в БД:**
```javascript
// В MongoDB shell
db.users.findOne({ email: "test@example.com" })
```

2. **Проверьте формат email:**
   - Email должен быть в lowercase (автоматически нормализуется)
   - Не должно быть пробелов
   - Должен соответствовать формату email

3. **Проверьте пароль:**
   - Пароль должен быть захеширован в БД
   - При логине передается оригинальный пароль (не хеш)
   - Пароль сравнивается через bcrypt.compare()

4. **Проверьте логи сервера:**
   - Должны быть сообщения об ошибках
   - Проверьте, что ошибка не в другом месте

### Проблема: "User with this email already exists"

**Это нормально**, если:
- Пользователь уже зарегистрирован
- Email уже используется другим пользователем

**Решение:**
- Используйте другой email
- Или войдите с существующим аккаунтом

### Проблема: "User with this username already exists"

**Это нормально**, если:
- Username уже занят

**Решение:**
- Используйте другой username

## 🧪 Тестирование

### Тест 1: Регистрация нового пользователя

```graphql
mutation {
  register(input: {
    username: "newuser"
    email: "newuser@test.com"
    password: "password123"
  }) {
    token
    user {
      id
      username
      email
    }
  }
}
```

**Ожидается:** Успех с токеном

### Тест 2: Логин с правильными данными

```graphql
mutation {
  login(input: {
    email: "newuser@test.com"
    password: "password123"
  }) {
    token
    user {
      id
      username
    }
  }
}
```

**Ожидается:** Успех с токеном

### Тест 3: Логин с неправильным паролем

```graphql
mutation {
  login(input: {
    email: "newuser@test.com"
    password: "wrongpassword"
  }) {
    token
  }
}
```

**Ожидается:** Ошибка "Invalid email or password"

## 🔧 Исправления в коде

### Что было исправлено:

1. ✅ **Нормализация email** - автоматическое приведение к lowercase
2. ✅ **Нормализация username** - удаление пробелов
3. ✅ **Улучшенная обработка ошибок** - конкретные сообщения
4. ✅ **Обработка ошибок MongoDB** - duplicate key errors
5. ✅ **Валидация входных данных** - проверка перед обработкой
6. ✅ **Логирование ошибок** - для отладки

### Код регистрации:

- Проверяет существование пользователя перед созданием
- Нормализует email и username
- Хеширует пароль перед сохранением
- Обрабатывает ошибки MongoDB (duplicate key)
- Возвращает понятные сообщения об ошибках

### Код логина:

- Нормализует email перед поиском
- Проверяет существование пользователя
- Сравнивает пароль через bcrypt
- Возвращает одинаковое сообщение для безопасности
- Обрабатывает ошибки с логированием

## 📝 Рекомендации

1. **Всегда используйте нормализованный email** - код делает это автоматически
2. **Проверяйте логи сервера** - там может быть больше информации
3. **Используйте правильный формат email** - должен содержать @ и домен
4. **Пароль минимум 6 символов** - проверяется на сервере
5. **Username минимум 3 символа** - проверяется в модели

## ✅ Проверка работоспособности

После исправлений должно работать:

- ✅ Регистрация нового пользователя
- ✅ Регистрация с существующим email - ошибка
- ✅ Регистрация с существующим username - ошибка
- ✅ Логин с правильными данными
- ✅ Логин с неправильным паролем - ошибка
- ✅ Логин с неправильным email - ошибка
- ✅ Логин с email в разном регистре - работает (нормализация)

